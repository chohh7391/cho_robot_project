{
  "_comment": [
    "Physics profile for OpenArm v1.0 under Isaac Sim, consumed by run_isaac_sim.py.",
    "Joint names, home pose and the hand's command interface must match the",
    "IsaacArmSystem / IsaacHandSystem blocks in",
    "cho_description_openarm/robots/openarm_v10/openarm_v10.ros2_control.xacro."
  ],
  "prim_path": "/openarm",
  "arm_joints": [
    "openarm_joint1",
    "openarm_joint2",
    "openarm_joint3",
    "openarm_joint4",
    "openarm_joint5",
    "openarm_joint6",
    "openarm_joint7"
  ],
  "hand_joints": [
    "openarm_finger_joint1",
    "openarm_finger_joint2"
  ],
  "_home_comment": "Matches the URDF <state_interface> initial_value entries. joint4's range is [0, 2.443], so 0.0 would rest it on the lower stop; 0.3 lifts it off. Same pose as home_position in cho_bringup_openarm/config/isaac/controllers.yaml.",
  "arm_home": [0.0, 0.0, 0.0, 0.3, 0.0, 0.0, 0.0],
  "hand_home": [0.0, 0.0],
  "_armature_comment": [
    "Rotor inertia (kg*m^2) for the Damiao motors, from the per-motor default",
    "classes enactic authored in openarm_mujoco v2/openarm_bimanual.xml:",
    "j1,j2 DM8009 0.0081  j3,j4 DM4340 0.1600  j5,j6 DM4310 0.0100  j7 DM3507 0.0049.",
    "This matters more here than on the FR3. Without armature the articulation's mass",
    "matrix is severely ill-conditioned: joint1 and joint3 rotate about axes that become",
    "parallel whenever joint2 is near zero, and the soft mode carries 6.7e-4 kg m^2",
    "against joint1's diagonal 1.2e-2 - 3.2e-4 at the worst pose in the workspace.",
    "On joints 3 and 4 the DM4340's 0.16 is fifteen times the link inertia itself.",
    "The v1 MuJoCo model this project simulates against declares none, which is why the",
    "MuJoCo bringup needs an inertia-weighted controller to stay stable."
  ],
  "arm_armature": [0.0081, 0.0081, 0.1600, 0.1600, 0.0100, 0.0100, 0.0049],
  "hand_armature": [0.001, 0.001],
  "_position_gain_comment": [
    "Isaac position-drive gains, per radian. Sized from the arm's own gravity load, not",
    "copied from another robot: k_i = max|tau_gravity,i| over the whole joint-limit box",
    "(40k Pinocchio samples of the URDF) divided by a 1.89 mrad per-joint sag budget.",
    "That budget is 0.015/sqrt(7)/3 - the action server compares the 7-joint error NORM",
    "against 0.015 rad, so seven joints share it, and 3x is the margin.",
    "Damping is critical for the drive alone, d_i = 2*sqrt(k_i * M_ii,max), with M_ii the",
    "diagonal inertia including the rotor at its worst pose. PhysX and Newton both",
    "integrate these drives implicitly, so high stiffness costs no discrete stability.",
    "The previous values [800,800,500,500,120,120,120] came from this project's MuJoCo",
    "actuator file (xml/openarm_v10/actuators_position.xml), where a position actuator's",
    "kp is a different quantity. They happened to pass at the usual test pose (worst",
    "joint 0.0058 rad) but leave a worst-case norm of 0.0198 rad, i.e. over threshold at",
    "the far end of the workspace; these give 0.0045.",
    "joint1 carries zero gravity torque at every pose (it rotates about the vertical), so",
    "it is sized with joint2 rather than left arbitrarily soft."
  ],
  "arm_position_stiffness": [6000.0, 6000.0, 2250.0, 2250.0, 300.0, 300.0, 300.0],
  "arm_position_damping": [103.0, 103.0, 48.0, 48.0, 4.4, 4.3, 3.5],
  "_velocity_gain_comment": "From actuators_velocity.xml kv.",
  "arm_velocity_damping": 25.0,
  "_torque_damping_comment": "Torque mode drives with damping only, because the controller's own damping arrives a ROS round trip late and the plant has to dissipate something. The per-motor joint damping values from openarm_mujoco v2, the only published source for them. Viscous, so it costs nothing at rest - what it buys is the dissipation the controller's own damping cannot supply until a ROS round trip later. Scaling them down to the Franka profile's 0.172 was tried and made the wrist markedly worse (j5-j7 went from 0.03-0.07 rad of residual to 0.15-0.23): those joints are light enough that the drive damping is most of what settles them.",
  "arm_torque_damping": [1.0, 1.0, 0.9, 0.9, 0.9, 0.9, 0.01],
  "_hand_gain_comment": "The gripper is position-driven in every control mode, as in MuJoCo. Prismatic (0 - 0.044 m), so N/m and N*s/m.",
  "hand_stiffness": 300.0,
  "hand_damping": 30.0,
  "_friction_comment": "PhysX joint friction COEFFICIENT, not a torque - it scales with the joint constraint force, so a value tuned on one arm does not transfer to another. 0.05 is what the Franka bringup measured, but the FR3 is roughly four times heavier; carried over unchanged it stalled openarm_joint1 at 0.013 rad of a 0.3 rad goal, because the drag on the joint carrying the whole arm exceeded the torque the inertia-weighted law commands there. 0.05 is what the Franka bringup measured, but the FR3 is roughly four times heavier; carried over unchanged it stalled openarm_joint1 at 0.013 rad of a 0.3 rad goal. 0.01 is the measured middle ground here, and it is not optional: PhysX friction is Coulomb, so it is what holds the arm still at rest. Setting it to 0 was tried and left the home pose drifting by 0.38 rad, with or without the drive damping - the same result the Franka profile records. The price is a standing offset the inertia-weighted law has to out-push, which is what sets the controller bandwidth in config/isaac/controllers.yaml.",
  "_newton_comment": [
    "PhysX-only keys, mostly. Newton ignores every physx* attribute here: it takes the drive",
    "gains from UsdPhysics.DriveAPI (authored before play, exactly as the PhysX path does -",
    "the per-degree scaling is right for both) and everything else from its own defaults.",
    "Those gains do double duty on Newton: its importer reads them to decide whether each DOF",
    "gets a POSITION actuator, a VELOCITY actuator or neither, and no runtime call revises",
    "that. A DOF with both gains zero gets no actuator and silently ignores position and",
    "velocity targets - which is why run_isaac_sim.py selects the 'physics' Physics variant",
    "for Newton rather than the 'mujoco' one Newton's own auto-switch picks: 'mujoco' carries",
    "no DriveAPI at all, so every DOF came out unactuated. Torque was unaffected (effort goes",
    "straight to joint_f and needs no actuator), which is exactly why it went unnoticed.",
    "arm_armature IS still applied at runtime through the tensor API: USD-authored",
    "'newton:armature' is deprecated in this release and physxJoint:armature is ignored.",
    "Two values have no Newton equivalent. joint_friction cannot be set at all - the",
    "articulation view's set_dof_friction_properties is a logged no-op in Isaac Sim 6.0.1 -",
    "so under Newton the joints carry no Coulomb friction. Newton also enforces no effort",
    "limit: it reports 1e6 Nm per DOF, so the controller's own clip_torque against the URDF",
    "limits is the only bound on a torque command.",
    "The missing friction helps rather than hurts: the standing offset it causes under PhysX",
    "is what forced the integral term in cho_bringup_openarm/config/isaac/controllers.yaml,",
    "and Newton holds a pose to 0.0000 rad where PhysX settles around 0.006-0.027.",
    "torque, position and velocity are all verified on both engines, single arm and bimanual.",
    "solver_position_iterations and solver_velocity_iterations are PhysX-only; Newton uses",
    "its own solver defaults."
  ],
  "joint_friction": 0.01,
  "ft_link": null,
  "solver_position_iterations": 32,
  "solver_velocity_iterations": 1,
  "enable_self_collisions": false
}
