# Robot configuration moved

Robot metadata is owned by `cho_robot_config/config/*.yaml`.  The compatibility
loader in `cho_task_manager.utils.controller_names` delegates to that registry;
do not add robot YAML files here.
