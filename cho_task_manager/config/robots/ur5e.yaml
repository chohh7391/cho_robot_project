# Single source of truth for UR5e controller roles.
# Loaded at runtime by cho_task_manager.utils.controller_names.load_robot_config().
robot_type: ur5e
controllers:
  joint_space: joint_space_position_controller
  task_space: task_space_ik_controller
  gripper: null
  vla: null
