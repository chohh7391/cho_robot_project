# Upstream provenance

This package adapts the Apache-2.0 licensed `franka_fr3_moveit_config` from
the vendored `extern/franka_ros2` tree.  The robot model remains owned by
`cho_description_franka`; only MoveIt-specific semantic, kinematics, planning,
limit and execution configuration is maintained here.
